"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      {" "}
      <button onClick={() => console.log("Click")}>Add to Chart</button>
    </div>
  );
};

export default AddToCart;
